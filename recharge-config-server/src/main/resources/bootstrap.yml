spring:
  application:
    name: config-server
  
  cloud:
    config:
      enabled: false
      server:
        git:
          uri: https://github.com/madhugithub2014/recharge-config.git
    
security:
  user:
    password: msr@ndona18
    
server:
  port: 8888